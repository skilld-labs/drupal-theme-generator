---
to: <%= h.src() %>/<%= h.changeCase.lower(name) %>/templates/components/suggestions/m-pager/m-pager.suggestion.yml
---
m_pager:
  use: '@component/m-pager/m-pager.html.twig'
  label: Molecules / Pager
  base hook: 'pager'
  libraries:
    - m-pager:
        css:
          component:
            m-pager.css: {}
